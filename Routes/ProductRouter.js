const route = require('express').Router()
const product = require('../Store/Store')

route.get('/product/:id', async(req,res)=>{
    const productId = request.params.id
    // const filterData = await 
})

module.exports = route

